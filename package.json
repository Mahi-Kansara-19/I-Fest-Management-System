{
  "name": "ifestapp",
  "version": "1.0.0",
  "description": "I-Fest Full Stack Project",
  "scripts": {
    "dev": "concurrently \"npm start --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
